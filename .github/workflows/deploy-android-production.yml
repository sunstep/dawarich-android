name: Build & Deploy Android (gms + foss) → Production [main]

on:
  push:
    branches: [ main ]
    paths:
      - "pubspec.yaml"
      - "lib/**"
      - "android/**"
      - "tools/**"
      - ".github/workflows/**"
      - "pubspec-foss.yaml"
      - "pubspec-foss.lock"
      - "pubspec-gms.yaml"
      - "pubspec-gms.lock"
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write

jobs:
  # ── Read version from pubspec (baked by update-build-number.yml on every dev push) ──
  # The build number is already committed into pubspec before this workflow runs,
  # so there is nothing to generate or bake here — just read and pass downstream.
  # Add [skip deploy] to a merge commit message to skip the Play upload entirely.
  setup:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[skip deploy]')"
    outputs:
      app_version: ${{ steps.ver.outputs.app_version }}
      build_number: ${{ steps.ver.outputs.build_number }}
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      - name: Read version from pubspec
        id: ver
        shell: bash
        run: |
          set -euo pipefail
          VLINE=$(grep '^version:' pubspec.yaml | head -1 | awk '{print $2}')
          APP_VERSION="${VLINE%%+*}"
          BUILD_NUMBER="${VLINE##*+}"
          echo "app_version=$APP_VERSION"   >> "$GITHUB_OUTPUT"
          echo "build_number=$BUILD_NUMBER" >> "$GITHUB_OUTPUT"
          echo "Version: ${APP_VERSION}+${BUILD_NUMBER}"

  # ── GMS build ──
  build-gms:
    needs: setup
    runs-on: ubuntu-latest
    env:
      JAVA_VERSION: "21"
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      - name: Setup Flutter (from .flutter-version)
        uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .flutter-version
          channel: stable
          cache: true

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Gradle wrapper validation
        uses: gradle/actions/wrapper-validation@v3

      - name: Write android/local.properties
        shell: bash
        run: |
          set -e
          SDK_DIR="${ANDROID_SDK_ROOT:-$ANDROID_HOME}"
          FLUTTER_SDK="${FLUTTER_HOME:-$(dirname "$(dirname "$(readlink -f "$(which flutter)")")")}"
          {
            echo "sdk.dir=${SDK_DIR}"
            echo "flutter.sdk=${FLUTTER_SDK}"
          } > android/local.properties

      # Cache NDK and CMake so Gradle never needs to download them mid-build.
      - name: Cache NDK 27 and CMake 3.22
        uses: actions/cache@v4
        id: cache-ndk-cmake
        with:
          path: |
            /usr/local/lib/android/sdk/ndk/27.0.12077973
            /usr/local/lib/android/sdk/cmake/3.22.1
          key: android-ndk-27.0.12077973-cmake-3.22.1-v1

      - name: Install NDK and CMake (if not cached)
        if: steps.cache-ndk-cmake.outputs.cache-hit != 'true'
        shell: bash
        run: |
          set -euo pipefail
          SDK_DIR="${ANDROID_SDK_ROOT:-$ANDROID_HOME}"
          "${SDK_DIR}/cmdline-tools/latest/bin/sdkmanager" \
            "ndk;27.0.12077973" \
            "cmake;3.22.1"

      - name: Pin NDK version in gradle.properties
        shell: bash
        run: |
          set -euo pipefail
          NDK_VERSION="27.0.12077973"
          GP=android/gradle.properties
          touch "${GP}"
          if grep -q '^android\.ndkVersion=' "${GP}"; then
            sed -i "s/^android\.ndkVersion=.*/android.ndkVersion=${NDK_VERSION}/" "${GP}"
          else
            printf '\nandroid.ndkVersion=%s\n' "${NDK_VERSION}" >> "${GP}"
          fi

      - name: Flutter pub get
        run: flutter pub get

      - name: Run build_runner
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Configure Gradle performance flags
        run: |
          sed -i '/^org\.gradle\.configuration-cache/d' android/gradle.properties || true
          cat >> android/gradle.properties <<'EOF'
          org.gradle.caching=true
          org.gradle.parallel=true
          org.gradle.daemon=true
          org.gradle.configuration-cache=false
          EOF

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Decode keystore
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: echo "$ANDROID_KEYSTORE_BASE64" | base64 -d > android/dawarich-upload.jks

      - name: Create keystore.properties
        run: |
          cat > android/keystore.properties <<'EOF'
          storePassword=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}
          keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}
          storeFile=../dawarich-upload.jks
          EOF

      - name: Build GMS AAB (release)
        run: |
          flutter build appbundle --release --flavor gms \
            --build-name=${{ needs.setup.outputs.app_version }} \
            --build-number=${{ needs.setup.outputs.build_number }}

      - name: Upload GMS artifact
        uses: actions/upload-artifact@v4
        with:
          name: gms-aab
          path: build/app/outputs/bundle/gmsRelease/*.aab
          retention-days: 30

  # ── FOSS build (parallel to GMS) ──
  build-foss:
    needs: setup
    runs-on: ubuntu-latest
    env:
      JAVA_VERSION: "21"
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      - name: Setup Flutter (from .flutter-version)
        uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .flutter-version
          channel: stable
          cache: true

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Gradle wrapper validation
        uses: gradle/actions/wrapper-validation@v3

      - name: Write android/local.properties
        shell: bash
        run: |
          set -e
          SDK_DIR="${ANDROID_SDK_ROOT:-$ANDROID_HOME}"
          FLUTTER_SDK="${FLUTTER_HOME:-$(dirname "$(dirname "$(readlink -f "$(which flutter)")")")}"
          {
            echo "sdk.dir=${SDK_DIR}"
            echo "flutter.sdk=${FLUTTER_SDK}"
          } > android/local.properties

      # Cache NDK and CMake so Gradle never needs to download them mid-build.
      - name: Cache NDK 27 and CMake 3.22
        uses: actions/cache@v4
        id: cache-ndk-cmake
        with:
          path: |
            /usr/local/lib/android/sdk/ndk/27.0.12077973
            /usr/local/lib/android/sdk/cmake/3.22.1
          key: android-ndk-27.0.12077973-cmake-3.22.1-v1

      - name: Install NDK and CMake (if not cached)
        if: steps.cache-ndk-cmake.outputs.cache-hit != 'true'
        shell: bash
        run: |
          set -euo pipefail
          SDK_DIR="${ANDROID_SDK_ROOT:-$ANDROID_HOME}"
          "${SDK_DIR}/cmdline-tools/latest/bin/sdkmanager" \
            "ndk;27.0.12077973" \
            "cmake;3.22.1"

      - name: Pin NDK version in gradle.properties
        shell: bash
        run: |
          set -euo pipefail
          NDK_VERSION="27.0.12077973"
          GP=android/gradle.properties
          touch "${GP}"
          if grep -q '^android\.ndkVersion=' "${GP}"; then
            sed -i "s/^android\.ndkVersion=.*/android.ndkVersion=${NDK_VERSION}/" "${GP}"
          else
            printf '\nandroid.ndkVersion=%s\n' "${NDK_VERSION}" >> "${GP}"
          fi

      - name: Switch pubspec to FOSS
        shell: bash
        run: |
          set -euo pipefail
          cp pubspec-foss.yaml pubspec.yaml
          cp pubspec-foss.lock pubspec.lock

      - name: Flutter pub get
        run: flutter pub get

      - name: Run build_runner
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Configure Gradle performance flags
        run: |
          sed -i '/^org\.gradle\.configuration-cache/d' android/gradle.properties || true
          cat >> android/gradle.properties <<'EOF'
          org.gradle.caching=true
          org.gradle.parallel=true
          org.gradle.daemon=true
          org.gradle.configuration-cache=false
          EOF

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: FOSS proprietary dependency check
        shell: bash
        run: |
          chmod +x tools/check_proprietary_deps.sh
          tools/check_proprietary_deps.sh fossReleaseRuntimeClasspath

      - name: Decode keystore
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: echo "$ANDROID_KEYSTORE_BASE64" | base64 -d > android/dawarich-upload.jks

      - name: Create keystore.properties
        run: |
          cat > android/keystore.properties <<'EOF'
          storePassword=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}
          keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}
          storeFile=../dawarich-upload.jks
          EOF

      # Cronet HTTP depends on GMS to provide a native adapter. cronetHttpNoPlay is a flag that embeds a prebuilt cronet
      # for non GMS builds/MicroG variants in order to make them work even without Google Services.
      - name: Build FOSS APK (release)
        run: |
          flutter build apk --release --flavor foss \
            --build-name=${{ needs.setup.outputs.app_version }} \
            --build-number=${{ needs.setup.outputs.build_number }} \
            --dart-define=cronetHttpNoPlay=true \
            --dart-define=geolocatorNoPlay=true

      - name: Upload FOSS artifact
        uses: actions/upload-artifact@v4
        with:
          name: foss-apk
          path: build/app/outputs/flutter-apk/app-foss-release.apk
          retention-days: 30

  upload-production:
    needs: [setup, build-gms, build-foss]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download GMS artifact
        uses: actions/download-artifact@v4
        with:
          name: gms-aab
          path: dist

      - name: Ensure whatsnew directory exists
        run: mkdir -p distribution/whatsnew

      - name: Upload to Play production (GMS)
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: ${{ secrets.PACKAGE_NAME_GMS }}
          releaseName: ${{ needs.setup.outputs.app_version }}+${{ needs.setup.outputs.build_number }}
          releaseFiles: dist/*.aab
          whatsNewDirectory: distribution/whatsnew
          track: production
          status: completed

